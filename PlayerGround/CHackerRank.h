#pragma once

#include <string>
#include <vector>

using namespace std;

class CHackerRank
{
public:
	static string reverseShuffleMerge(string s);
	static void whatFlavors(vector<int> cost, int money);
	static vector<vector<int>> swapNodes(vector<vector<int>> indexes, vector<int> queries);
};

